// Placeholder. Los campos reales se mapearan al integrar la API de FacturaScripts.
export interface Cliente {
  codcliente?: string;
  nombre?: string;
  cifnif?: string;
  email?: string;
  [key: string]: unknown;
}
